<div class="flash">
	<%= flash[:notice] %>
</div>

<h1>Books</h1>

<table>
    <thead>
        <tr>
            <th>Title</th>
            <th>Body</th>
            <th colspan="3"></th>
        </tr>
    </thead>
    <tbody>
        <% @books.each do |book| %>
            <tr>
                <td><%= book.title %></td>
                <td><%= book.body %></td>
                <td><%= link_to "Show", book_path(book), class: "link" %></td>
                <td><%= link_to "Edit", edit_book_path(book), class: "link" %></td>
                <td><%= link_to "Destroy", book_path(book), class: "link", method: :delete, "data-confirm" => "Are you sure?" %></td>
            </tr>
        <% end %>
    </tbody>
</table>

<h2>New book</h2>
    <div class="errors">
      <% if @book.errors.any? %>
        <%= @book.errors.count %>error prohibited this book from being saved:
        <% @book.errors.full_messages.each do |message| %><br>
          ・<%= message %>
        <% end %>
      <% end %>
    </div>

  <%= form_with model: @book, local:true do |f| %>

    <div class="field">
      <%= f.label :Title, class: "mt-3 mr-5" %><br>
      <%= f.text_field :title, :class => "book_title" %>
    </div>

    <div class="field">
      <%= f.label :Body, class: "mt-3 mr-5" %><br>
      <%= f.text_area :body, :class => "book_body" %>
    </div>

    <div class="actions">
      <%= f.submit 'Create Book' %>
    </div>

  <% end %>